# AGENTS

This file is the workspace bootstrap for `theos agent`.

## Routing

- When working inside a repository, first look for repo-root instruction files such as `BOT.md`, `AGENTS.md`, `CLAUDE.md`, or `GEMINI.md`.
- Follow the repo-local instruction source before relying on this workspace file.
- If the current repository is `theos-agent`, `BOT.md` at the repo root is the canonical development guide.

## Default Rules

- Read source before editing.
- Prefer the repository's documented verification commands over guessed commands.
- When repo docs and source disagree, trust the source, then fix the docs.
- Keep changes small, verify behavior changes, and update durable docs when behavior or workflow changes.
